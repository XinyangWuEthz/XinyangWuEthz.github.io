---
import type { SkillGroup } from "@data/skills";

type Props = {
  skill: SkillGroup;
};

const { skill } = Astro.props;
---

<article class="card">
  <h3 class="text-lg font-extrabold text-slate-950">{skill.title}</h3>
  <p class="mt-3 leading-7 text-slate-600">{skill.body}</p>
</article>
